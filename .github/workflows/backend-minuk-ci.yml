name: Minuk Backend CI/CD

on:
  push:
    branches: [ "main", "dev" ]
    paths:
      - 'minuk/**'
      - 'Dockerfile.minuk'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'minuk/**'
      - 'Dockerfile.minuk'

jobs:
  test_and_build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
      
    - name: Set up Python 3.10
      uses: actions/setup-python@v5
      with:
        python-version: "3.10"
        cache: 'pip'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r minuk/ai_service/requirements.txt
        
    - name: Run tests (if any)
      run: |
        if [ -d "minuk/tests" ]; then pytest minuk/tests/ -v; else echo "No tests directory found, skipping."; fi

    - name: Set up Docker Buildx
      if: github.ref == 'refs/heads/main'
      uses: docker/setup-buildx-action@v3
      
    - name: Build Docker Image
      if: github.ref == 'refs/heads/main'
      uses: docker/build-push-action@v5
      with:
        context: .
        file: Dockerfile.minuk
        push: false
        tags: multimodal-validation-minuk:latest
