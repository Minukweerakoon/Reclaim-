# Dependencies
node_modules/

# Environment (secrets, keys)
.env
.env.local
.env.*.local

# Lock file (optional: remove if you want to commit for reproducible installs)
package-lock.json

# Uploaded files (Voshan videos, etc.)
uploads/
